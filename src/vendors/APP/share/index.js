var title = data.title;
var content = data.title;
var imgUrl = data.avatar;
var titleUrl= location.href;
var url= location.href;
var siteUrl= location.href;

appShare(title, content, imgUrl, titleUrl, url, siteUrl);